return function(self, params)
	print("self", self.inspect(self), "params", self.inspect(params))
	return {
		{
			word = "$dnr-line-height-100",
			label = "$dnr-line-height-100 100%;",
			insertText = "$dnr-line-height-100",
			filterText = "$dnr-line-height-100 100%;",
		},
	}
end
